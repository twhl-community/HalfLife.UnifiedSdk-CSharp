namespace HalfLife.UnifiedSdk.Utilities.Games
{
    /// <summary>Provides information about a map, such as which type of map it is.</summary>
    public record MapInfo(string Name, MapCategory Category);
}
